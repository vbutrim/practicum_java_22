package com.vbutrim.filmorate.api;

import org.junit.jupiter.api.Test;

import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import javax.validation.Validation;
import javax.validation.Validator;
import javax.validation.ValidatorFactory;

import java.util.Set;

import static org.junit.jupiter.api.Assertions.*;

class FilmControllerTest {

    @Test
    public void should() {
/*        // Given
        FilmController.FilmToCreateDto filmToCreateDto = new FilmController.FilmToCreateDto();
        FilmController filmController = new FilmController();

        ValidatorFactory factory = Validation.buildDefaultValidatorFactory();
        Validator validator = factory.getValidator();
        Set<ConstraintViolation<FilmController.FilmToCreateDto>> violations = validator.validate(filmToCreateDto);
        if (!violations.isEmpty()) {
            throw new ConstraintViolationException(violations);
        }

        // When
        filmController.createFilm(filmToCreateDto);

        // Then*/
    }

}